<div class="row">
    <table>
        <tr>
            <th>アーティスト名</th>
            <th></th>
            <th></th>
        </tr>
        <% @artists.each do |artist| %>
            <tr>
                <td><%= artist.name %></td>
                <td><%= link_to '編集', edit_admin_artist_path(artist) %></td>
                <td><%= link_to '削除', admin_artist_path(artist), method: :delete %></td>
            </tr>
        <% end %>
    </table>

    <div>
        <%= link_to 'アーティスト追加', new_admin_artist_path %>
    </div>
</div>